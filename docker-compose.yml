services:
  database:
    build:
      context: ./Backend/Database
    ports:
      - "5432:5432"
  backend:
    build:
      context: ./Backend/WebApi
    ports:
      - "5000:5000"
    depends_on:
      - database
  frontend:
    build:
      context: ./Frontend
    ports:
      - "80:3000"